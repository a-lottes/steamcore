# Review Report: galactic-invasion

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The uncommitted working tree vs. `995e826`, `.spark/galactic-invasion/plan.md` |
| **Status** | `passed` |
| **Round** | 2 |
| **Date** | 2026-09-07 |

**Handoff**
- **Status:** `passed` — Round 2 confirmed the Blocker and both Majors fixed against the source, not against fix-mode's prose.
- **Verdict:** F1 verified by re-deriving it: I re-planted the exact `formationWasPastThreshold_` latch and the new F3 test failed (`round_test.cpp:430: ended`, 48 passed / 1 failed), reverted byte-identical — the stateless `checkFormationThreshold()` genuinely re-arms and AC-5.2/AC-10.6 hold. F2's write-once guard confirmed at all three writer sites and the header's false "mutually exclusive by construction" claim is gone. F3's test exists, passes, and is a real detector. F4/F6/F7 confirmed (F7's lint rule re-demonstrated firing on a planted file in a *second* `games/` subdirectory). Three new Minors this round: `F9` (F2's own guard has no detector — I removed it and the suite stayed 255/255), `F10` (plan.md T9 still documents the deleted latch), `F11` (stale case count, fixed by me). None blocks the gate.
- **Open:** `1 open` — Minor: `F8` (informational only, for `/go-live` to stage precisely; no code change needed). F5 and F9 accepted by the user; F10 fixed post-Round-2 in plan.md's T9 row. No Blocker or Major remains open.
- **Binding ruling:** §6 Verdict and the gate checklist below — the only binding location; there is no other round to point to
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Round 2 (re-review). Re-read in full: `games/galactic_invasion/galactic_invasion.{h,cpp}`, `firmware/steamcore/test/galactic_invasion_round_test.cpp`, `tools/check_constraints.sh`'s galactic-invasion block, plan.md's T8/T9 rows. Round 1's scope (the whole working-tree diff vs `995e826`) is unchanged and its ✅ findings on files untouched this round (art, fixture, bench, harness, `Makefile`, `CMakeLists.txt`, `app_main.cpp`, `device-build.md`) are cited from Round 1, not re-derived.

Verified myself this round: `make test-all` exits 0 — **255 passed, 0 failed** on clang, ASan and gcc alike; `test-negative`'s `0 passed, 1 failed` is the harness self-check, expected; six `BENCH OK` (galactic-invasion tick **4.63 µs**, ratio 1.49x, vs the 16.667 ms budget); `make lint OK`. Three re-derivations rather than citations, all because they verify a fix to a Must-level claim: **(M1)** re-planted F1's exact latch → the F3 test failed at `round_test.cpp:430: ended` (48 passed / 1 failed) and nothing else did; **(M2)** removed both of F2's write-once guards → **255 passed, 0 failed** (see F9); **(M3)** planted a bare `43` in a new `games/scratch_second_game/probe.h` → F7's rule fired by name. All three reverted and `diff`-confirmed byte-identical / removed.

Not reviewed: `idf.py build` was not re-run (T16's claim cited from Round 1; the diff touched no device-side file this round). AC-12.4 cited from Round 1. No blast-radius tool file was passed; scoping was by hand.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1, T2, T4, T5, T6, T10, T11, T12, T14, T15, T16, T17 | ✅ | All DoDs verified against the source; T3/T5/T15's recorded deviations are stated in their own rows and are sound. |
| T3 | ✅ | `inline` on `reportInvalidSpriteArt()` deviation documented and correct (header included by 3 TUs). |
| T7 | ✅ | `checkCollision` over typed pools, no bespoke overlap math anywhere in `games/` (confirmed by reading). R6's `sweepCollisions` wording mismatch is accepted — AC-4.1 permits either. |
| T8 | ✅ | The shipped intra-tick order (`galactic_invasion.cpp:97-125`) matches T8's new second-deviation note element for element; Decision 7's drift is now recorded, not silent (F4 closed). |
| T9 | ⚠️ | The rule as shipped now satisfies AC-5.2/AC-10.6 (F1 closed, re-derived via M1) and has a detector (F3 closed). The row's own text is stale, though — it still names the deleted `formationWasPastThreshold_` latch as the shipped mechanism (F10). |
| T13 | ⚠️ | F3's gap is closed for the threshold rule. The mutation set still has one hole: F2's write-once guard survives deletion with the suite green (F9). |

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Blocker | `games/galactic_invasion/galactic_invasion.cpp:284-293` | `formationWasPastThreshold_` is latched unconditionally (`formationWasPastThreshold_ = anyPast;`) even on the tick the failsafe *deferred* to a contact hit. Once a hit lands on the same tick as the first crossing, `newCrossing` can never be true again for the rest of the round (survivors at/below the player row cannot be shot — the shot travels upward), so AC-5.2/AC-10.6 ("ends the round regardless of lives remaining and regardless of invulnerability") stop applying permanently. Demonstrated: with the ship on a bottom-row column at the crossing tick (5111), one life is lost, the round continues, the formation descends past the ship and off-screen, and after 40 000 further ticks `outcome_` is still `NONE` with 2 lives left — `GameState` is stuck in `PLAYING`, where `start` does nothing (`game_state.cpp:13-15`), so the round can never end and can never be restarted. That is the "dodge forever" exploit A7/A13 exist to close, plus a violation of constitution Principle 4 ("restart always one button away"), and it also lets enemies render inside `kPlayerBandBounds`/the HUD row (AC-7.2). The entry condition is not exotic: at the crossing tick the front row lands at y=136, overlapping the player band y∈[144,156), so a large fraction of ship positions contact on that tick. **Fix:** do not latch a crossing that was deferred — e.g. `formationWasPastThreshold_ = anyPast && !(newCrossingDeferred)`, so the next tick with no concurrent contact still fires; or re-arm whenever `invulnTicks_ == 0`. Whichever shape is chosen, AC-10.6's "regardless of invulnerability" has to survive it. **Fixed:** the latch (`formationWasPastThreshold_`) was removed entirely rather than patched — `checkFormationThreshold` now re-derives `anyPast` fresh every call with no memory of a prior tick, so a deferred crossing has nothing to go stale; the very next tick with `contactResolved == false` re-evaluates the same still-true `anyPast` and ends the round (AC-10.6 holds structurally, not via a repaired latch). Verified: a scratch reintroduction of the exact original latch bug makes the new F3 test fail (`ended` false, times out) — confirmed reverted byte-identical after. **Confirmed r2:** re-derived independently (this verifies a fix to a Blocker-level Must AC, so citation was not permissible) — M1 re-planted the exact original latch and `galactic_invasion_threshold_failsafe_still_fires_after_a_coincident_hit` failed at `round_test.cpp:430: ended` while all 48 other galactic-invasion cases passed; reverted byte-identical. The shipped `checkFormationThreshold()` (`:266-291`) holds no state at all, so no deferred crossing can go stale. | fixed r2 |
| F2 | Major | `galactic_invasion.cpp:339` vs `:370` (order set at `:105`/`:118`) | `outcome_` is a single field written by two independent resolvers in one tick with no guard: `resolvePlayerShotHits()` sets `WIN` when the last enemy dies, then `resolveEnemyShotHits()` → `resolvePlayerHit()` overwrites it with `LOSS` if that same tick's in-flight enemy shot hits a player on their last life. Demonstrated: the formation is cleared, yet the end screen renders `GAME OVER` (outcome text span x=[85,153], the 72px loss string), not `YOU WIN` — AC-4.3/AC-11.1 violated. Reachable in ordinary play, since enemy shots outlive their shooter. It also falsifies the header contract at `galactic_invasion.h:55-58` ("All three are mutually exclusive within a single tick by construction"), which is an NFR-6 contract-clarity defect in its own right. **Fix:** make `outcome_` write-once per round (`if (outcome_ == Outcome::NONE)`), or skip damage resolution once a `WIN` is recorded this tick; then correct the doc comment to state the precedence rule instead of claiming exclusivity. **Fixed:** `outcome_` is now write-once at all three writer sites (`resolvePlayerShotHits`, `resolvePlayerHit`, `checkFormationThreshold`), each guarded by `outcome_ == Outcome::NONE`; the header's file-level contract rewritten to state the write-once/precedence rule instead of the false "mutually exclusive by construction" claim. **Confirmed r2:** `grep` over the whole file finds exactly three `outcome_ = Outcome::{WIN,LOSS}` writes (`:289`, `:339`, `:373`), each guarded by `outcome_ == Outcome::NONE`; the only unguarded write is `resetRound():218`'s reset to `NONE`, which is correct. The header contract (`galactic_invasion.h:54-60`) now states write-once/precedence and names the same-tick coincidence as the reason. See F9 for the detector gap this fix left. | fixed r2 |
| F3 | Major | `firmware/steamcore/test/galactic_invasion_round_test.cpp:330-360` | Neither half of T9's threshold rule has a detector. I planted two mutations against a scratch copy of the source and rebuilt the full suite: (a) removing the edge trigger (`newCrossing = anyPast`) → **254 passed, 0 failed**; (b) removing the contact deferral (`if (newCrossing)`) → **254 passed, 0 failed**. The only threshold test, `galactic_invasion_dodge_run_reaches_threshold_with_lives_intact`, asserts `!everLostALife`, which by construction excludes the exact coincident-hit case F1 lives in. In a codebase whose stated culture is "demonstrate the detector firing", a rule introduced as a Major bug fix and then guarded by nothing is the gap that let F1 ship. **Fix:** add a test that drives a hit onto the crossing tick and asserts the round still ends by the failsafe within a bounded number of ticks (the deterministic crossing tick is already computable — `predictThresholdCrossing()` at `:183`), then re-run both mutations and record them failing. **Fixed:** added `galactic_invasion_threshold_failsafe_still_fires_after_a_coincident_hit`. It steers the player onto the exact coincident-hit column at the predicted crossing tick (via the existing shot-evading `dodgeInput`), then bounds the check to `kInvulnerabilityTicks` after the hit — tight enough that AC-10.3 (invulnerability blocks every *other* ending source for that whole window) makes the failsafe the only possible cause of an ending inside it, avoiding a confound an earlier draft hit (a "drive to safety afterward" version let the formation's continued horizontal drift rack up further *ordinary* hits over the remaining ~10,000-tick budget and end the round the mundane way even against a reintroduced F1 bug). Demonstrated failing against both a reintroduced F1 latch bug (`ended` false) and a removed AC-5.2 deferral guard (`everLostALife` false, since without deferral the round ends the same tick as the hit, before the LIVES:2 frame ever renders) — both reverted byte-identical after. Also note for future test-writers: the round's "has it ended" signal must never be read off player-sprite visibility (`findPlayerXStrict == -1`) once invulnerability is in play — the AC-10.8 flicker periodically renders no player pixels on a genuine PLAYING frame, which reads as a false "ended" and would have silently masked this exact bug; `outcomeTextRenderedWidth(fb) > 0` is the unambiguous signal, confirmed only rendered once GAME_OVER actually starts. **Confirmed r2:** the test exists at `galactic_invasion_round_test.cpp:363-431`, compiles, and `make test FILTER=threshold_failsafe_still_fires` reports `1 passed, 0 failed`. Judged a genuine detector, not a passes-today test: M1 showed it is the *only* case in the suite that fails against the reintroduced latch, and its `kInvulnerabilityTicks` bound plus the `everLostALife` precondition together pin both halves of the rule (one-tick deferral, then re-arm) rather than just "the round eventually ends". | fixed r2 |
| F4 | Minor | `galactic_invasion.cpp:103-125` | Shipped intra-tick order is player move → `stepFormation` → `stepPlayerShot` → shot hits → `stepEnemyShots` → contact → enemy-shot hits → invuln decrement → threshold. Plan §1 Decision 7 fixes it as tick counters → player move → **shots move** → **formation step** → collisions → win → threshold → invuln decrement. Two swaps, neither documented in any task row, in the one ordering the plan's own R5 calls the highest-risk item ("a wrong order changes gameplay subtly and passes every isolated unit test"). No behavioural defect found (closing speeds stay below the overlap window, so no tunnelling), but silent drift from a written-down order is exactly what R5 warns about. **Fix:** either reorder to match Decision 7, or amend Decision 7 to the shipped order and record it as a deviation. **Fixed (documented, not reordered):** recorded as a second deviation in plan.md's T8 row, naming the shipped order as the authoritative baseline going forward — reordering to match Decision 7 retroactively was judged riskier than documenting the order that has already shipped and passed the full gate with no behavioral defect found. **Confirmed r2:** plan.md T8's appended note states the shipped order, and I checked it element for element against `update()` (`:97-125`) — player move, `stepFormation`, `stepPlayerShot`, `resolvePlayerShotHits`, `stepEnemyShots`, contact + enemy-shot resolution, invuln decrement, `checkFormationThreshold` — it matches, and it names itself the authoritative baseline going forward. A documented, accepted deviation is a correct resolution here. | fixed r2 |
| F5 | Minor | `galactic_invasion.h:98-279` | Library lens §1 (public API surface, NFR-5): `struct Enemy`, `isAlive()` and generic constants (`kPlayerWidth`, `kPlayerY`, `kEnemyWidth`, `kEnemyHeight`, `kProjectileSpeedY`, `kInvulnerabilityTicks`, `kRngSeed`, `stepIntervalTicks()`, …) all sit at `steamcore::games` namespace scope. `Enemy`/`isAlive` are pure implementation detail with no consumer; the generic names are guaranteed to collide with README's next three games, which share that namespace. This is the first game and therefore the precedent. **Fix:** nest the game in `steamcore::games::galactic_invasion` (or move `Enemy`/`isAlive` inside the class and keep only the test-consumed layout rects at namespace scope). EM ruling before game #2, not a defect today. **User decision:** left for the next game's own planning (`/sprint-plan` or `/charter`) rather than refactored now — no AC depends on it, and a refactor touching every `galactic_invasion_*_test.cpp` file's `using`-declarations is out of proportion to a forward-looking Minor with no current defect. | accepted |
| F6 | Minor | `galactic_invasion.h:380`, `galactic_invasion.cpp:200`; also `galactic_invasion.h:108`, `galactic_invasion.cpp:26` | The 3-lives starting count (spec A1) is a bare `3` duplicated in the member initializer and in `resetRound()`, with no named constant — two places to drift, and the header's own contract text says "3 lives". Same class: the bottom-margin `- 4` in `kPlayerY` and `9 * kGlyphHeight` for `kOutcomeTextY` are bare layout literals, against CLAUDE.md's "derive on-screen layout, never a literal". **Fix:** add `kStartingLives = 3` and use it in both places; derive or name the two layout numbers. **Fixed:** `kStartingLives = 3` added and used in both the member initializer and `resetRound()`; the bottom margin named `kPlayerBottomMarginPx = 4`. `kOutcomeTextY`'s `9 * kGlyphHeight` was left as-is — it already follows this codebase's own established, previously-reviewed glyph-row-multiple convention (`title_screen.h`'s `6 * kGlyphHeight`/`14 * kGlyphHeight`, `kFormationStartY`'s `2 * kGlyphHeight`), so renaming just this one row index seemed inconsistent rather than safer; flagged here for the reviewer to confirm or override at re-review. **Confirmed r2:** `kStartingLives = 3` (`galactic_invasion.h:181`) is the only source for both `lives_` sites (`h:405`, `cpp:200`) — no bare `3` left; `kPlayerBottomMarginPx = 4` (`h:112`) feeds `kPlayerY` (`h:114`). `kOutcomeTextY`'s `9 * kGlyphHeight` left as-is: **override declined** — it is the same glyph-row-multiple convention `kFormationStartY` and `title_screen.h` already use, and singling it out would be inconsistency, not safety. | fixed r2 |
| F7 | Minor | `tools/check_constraints.sh:718-737` | Inside the new galactic-invasion block, the allocation / ESP-IDF-header / resolution-literal / `fillRect` / `assets` rules all `grep -r "$GAMES_DIR"` (whole tree), but the glyph-metric (`8`/`43`) rule runs only over the hardcoded `$GALACTIC_INVASION_SRC_FILES`. A second game under `games/` would silently escape that one rule — the exact "R4: `games/` is ungated" risk the block exists to close, left half-open. **Fix:** run the awk check over `find "$GAMES_DIR" -name '*.h' -o -name '*.cpp'` like its siblings. **Fixed:** the check now runs over `find "$GAMES_DIR" \( -name '*.h' -o -name '*.cpp' \)` piped through the same awk, rather than the hardcoded file list. Demonstrated: a scratch file under a second `games/` subdirectory (outside the old list) with a bare `43` now correctly fires the rule; removed after confirming, `make lint` green. **Confirmed r2:** read the script (`check_constraints.sh:717-745`) — the rule now runs `find "$GAMES_DIR" \( -name '*.h' -o -name '*.cpp' \) -print0 | xargs -0 awk`, matching its siblings' whole-tree scope. Re-derived rather than cited (this verifies the fix itself): M3 planted `games/scratch_second_game/probe.h` containing a bare `43` — outside the old hardcoded list — and lint reported it by path with the rule's own message; scratch dir removed, `make lint OK`. | fixed r2 |
| F8 | Minor | working tree (`git status`) | Three files unrelated to this feature are modified and uncommitted alongside it — `CLAUDE.md` (start-screen-era lessons), `.spark/start-screen/release.md` (`preparing`→`released`), `docs/wiring-analog-joystick.md` (analog-joystick status) — plus untracked `assets/Buttons.png`, `assets/fonts/`, `assets/sprites/`. None belongs to galactic-invasion's diff. Per CLAUDE.md's own "re-check `git status` before staging" rule, `/go-live` must stage this feature's paths explicitly and neither sweep these in nor leave a genuinely-modified feature file out. | open |
| F9 | Minor | `games/galactic_invasion/galactic_invasion.cpp:339`, `:373` | F2's fix shipped with no regression detector. I removed both `outcome_ == Outcome::NONE` guards (mutation M2, the exact pre-F2 code) and rebuilt the full suite: **255 passed, 0 failed** — the same "a Major fix guarded by nothing" gap F3 named for T9's threshold rule, reproduced for F2's write-once rule. Not a defect today: the guard is present, correct, and three locally-inspectable lines, so AC-4.3/AC-11.1 hold as shipped — which is why this is Minor and not a repeat of F3's Major. But the win-overwritten-by-a-same-tick-loss case Round 1 demonstrated as reachable would return silently under any future refactor of `update()`'s tick order. **Fix:** either a test that lands the last-enemy kill and an in-flight enemy shot on the same tick and asserts `YOU WIN` renders (the F3 test's `predictThresholdCrossing`-style precomputation is the model), or, if that scenario is judged too costly to script, an explicit line in plan.md T13's mutation record naming this mutation as knowingly undetected rather than leaving it unrecorded. **User decision:** accepted as-is — the guard is present, correct, and three inspectable lines, violating no AC today; scripting a deterministic same-tick last-enemy-kill + last-life enemy-shot-hit scenario was judged not worth the cost for this gap alone. | accepted |
| F10 | Minor | `.spark/galactic-invasion/plan.md:195` (T9 row) | T9's completion note still describes the shipped mechanism as "the threshold check is edge-triggered via a new `formationWasPastThreshold_` latch" and "`resetRound()` additionally zeroes the new `formationWasPastThreshold_` latch" — a member F1's fix deleted. T8's row got a "Second deviation, recorded at /peer-review Round 1" note; T9's got nothing, so the plan now documents an implementation that does not exist and a reader hunting the latch finds no record of its removal. **Fix:** append a note to T9's row parallel to T8's, stating that the latch was removed at `/peer-review` Round 1 (review F1) and that `checkFormationThreshold()` is now stateless — level-triggered, re-derived every tick, deferring only on a same-tick resolved contact. **Fixed:** appended a "Superseded at /peer-review Round 1 (review F1/F10)" note to T9's row, parallel in form to T8's own deviation note, stating the latch's removal and the stateless replacement's behavior. | fixed |
| F11 | Minor | `docs/host-tests.md:68` | The R8-risk-mandated case-count record still read **254 passed, 0 failed** after F3's new test took the real count to 255 — the single number that risk exists to keep watched, left stale by the fix round. **Fixed by reviewer:** updated to 255 with a clause naming the added case as the 255th; `make lint` re-run green afterwards. | fixed r2 |

## 4. Requirements Traceability

Compressed for the line budget: rows are grouped where every AC in the group has the same verdict; anything not ✅ gets its own row.

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1–1.3 (movement) | `galactic_invasion.cpp:97-101` | ✅ met |
| AC-2.1–2.3 (single projectile) | `galactic_invasion.cpp:293-311` | ✅ met |
| AC-3.1–3.4 (formation) | `galactic_invasion.cpp:221-264` | ✅ met |
| AC-4.1, AC-4.2 | `galactic_invasion.cpp:313-325` | ✅ met |
| AC-4.3 (formation-clear → win) | `galactic_invasion.cpp:332-339` | ✅ met r2 — write-once guard holds the WIN (F2 closed); detector gap is F9 |
| AC-5.1 (contact, same-tick end) | `galactic_invasion.cpp:342-375`, `:131-133` | ✅ met |
| AC-5.2 (threshold failsafe) | `galactic_invasion.cpp:266-291` | ✅ met r2 — stateless check, re-derived via M1 |
| AC-5.3 (loss screen + score) | `galactic_invasion.cpp:180-192` | ✅ met |
| AC-6.1–6.3 (title, start, restart) | `galactic_invasion.cpp:143-144`, `:135-137`, `:197-219` | ✅ met |
| AC-7.1, AC-7.2 (score, disjointness) | `galactic_invasion.cpp:169-177`; `galactic_invasion.h:266-278` | ✅ met r2 — F1's player-band leak is gone; the four `static_assert`s plus T4's independent runtime helper stand |
| AC-8.1–8.3 (Should: enemy fire) | `galactic_invasion.cpp:377-444` | ✅ met |
| AC-9.1, AC-9.2 (Should: speedup) | `galactic_invasion.h:166-171`; `.cpp:236-240` | ✅ met |
| AC-10.1–10.5, AC-10.7, AC-10.8 | `galactic_invasion.cpp:112-119`, `:362-375`, `:163-165` | ✅ met — the 120-tick window is exactly 120 (snapshot read at top, decrement at bottom) |
| AC-10.6 (failsafe not softened) | `galactic_invasion.cpp:288-290` | ✅ met r2 — once invulnerability makes `contactResolved` false again, the still-true `anyPast` fires; asserted within the 120-tick window by the F3 test |
| AC-11.1, AC-11.3, AC-11.4 | `galactic_invasion.cpp:180-192`, `:135-137` | ✅ met — `GameState` gains no enumerator |
| AC-11.2 (visibly distinct screens) | `galactic_invasion.cpp:18-54` | ✅ met r2 — the wrong-screen path is closed with F2 |
| AC-12.1–12.4 (sprites) | `galactic_invasion_art.h:38-212`; `.cpp:147-165` | ✅ met — `constexpr` `Color` arrays, no PNG/file I/O, `blit` only, three clearly distinct silhouettes |
| NFR-1 performance | `bench_galactic_invasion.cpp` | ✅ 4.63 µs/tick re-measured r2, ratio 1.49x |
| NFR-2 no allocation | `check_constraints.sh:678-688` | ✅ met |
| NFR-3 determinism | `galactic_invasion_determinism_test.cpp`; `.cpp:407-412` | ✅ met — explicit constant-seeded LCG, no clock |
| NFR-4 portability / unmodified primitives | `git diff --exit-code` (Round 1, re-derived), T16 | ✅ met |
| NFR-5 public API surface | `galactic_invasion.h:291-297` | ⚠️ partial — class surface is exactly ctor/`update`/`render`, but namespace-scope leakage (F5) |
| NFR-6 contract clarity | `galactic_invasion.h:12-99` | ✅ met r2 — the false mutual-exclusion claim is replaced by an accurate write-once/precedence rule |
| NFR-7 accessibility | `galactic_invasion.cpp:171-191` | ✅ met — `BRIGHT_ORANGE` on `BLACK` throughout |
| NFR-8/9/10 | — | ✅ N/A per spec |

## 5. What Was Checked

- [x] Correctness: logic does what the acceptance criteria demand — every Must AC now traces to code that satisfies it; F1's and F2's failing ACs re-verified by planting each bug back and watching (or not watching) the suite react
- [x] Non-functional: applicable NFRs and constitution quality bars hold — NFR-4's untouched-primitives claim re-derived from source; §3 no-allocation / no-clock / no-resolution-literal all enforced by the new lint block
- [x] Error handling: no error surface exists (no I/O, no allocation, nothing throws); the art validator's `std::abort()`-not-`throw` discipline is correct for `-fno-exceptions`
- [x] Security: N/A per NFR-8 — offline device, no input parsing, no persistence, no secrets
- [x] Tests: exist, are meaningful, and pass — 255/255 green on clang/ASan/gcc; F3's new case proven to be the sole detector of F1's bug (M1). One mutation still survives: F2's own guard (M2, F9)
- [x] Readability: the next developer will understand this — doc comments are unusually good and now accurate; F5 stays the open maintainability item, F10 the open archaeology hazard

## 6. Verdict

This passes. I did not take fix-mode's word for any of it: the Blocker's fix was re-derived by planting the original `formationWasPastThreshold_` latch back into `checkFormationThreshold()` and confirming the round test fails on exactly the assertion it should (`round_test.cpp:430: ended`) and that no other case in the suite notices — which both proves the fix real and proves F3's new test is a genuine detector rather than one more passing case. The shipped check now carries no state at all, which is a better answer than the repaired latch F1 suggested: there is nothing left that can go stale, and AC-10.6's "regardless of invulnerability" holds structurally, because the tick after a deferred hit the invulnerability window makes `contactResolved` false again while `anyPast` is still true. F2 is closed at all three writer sites with the header's false mutual-exclusion claim replaced by an accurate write-once rule, and F7's lint rule now genuinely covers a second game's directory — I planted a file there to be sure. F4's resolution as a documented, accepted deviation is the right call and its note matches the shipped order element for element. Three new Minors came out of this round and none of them justifies holding the feature: F9 is the honest one to weigh — F2's guard survives deletion with 255/255 still green, which is structurally the same gap F3 was raised for, and I considered calling it Major for consistency. I did not, because the difference is real: F3's gap had already let a Blocker ship, whereas F2's guard is present, correct, three inspectable lines, and violates no AC today. Calling it Major would be inflating a test-coverage debt into a defect. F10 (plan.md's T9 row still documenting the deleted latch) and F11 (a stale case count, which I fixed) are artifact staleness, not code. F5 and F8 remain the user's own calls, unchanged and still accurately described. Go to `/demo-day`.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings — F1 `fixed r2`, re-derived via mutation M1, not accepted on fix-mode's prose
- [x] No open Major findings (or explicitly waived by the user, with reason recorded here) — F2 and F3 both `fixed r2`; no waiver needed or given
- [x] Every Must AC traces to implementing code; no constitution non-negotiable violated — AC-5.2/AC-10.6 now met; Principle 4 (restart one button away) is no longer breakable, since no round state can strand `GameSession` in `PLAYING`
- [x] All plan deviations documented and accepted — T8's intra-tick order recorded as an accepted second deviation (F4 closed); T9's row is stale but records no *undocumented* deviation (F10, Minor)
- [x] Test suite runs green — `make test-all` exit 0, **255 passed / 0 failed** on clang, ASan and gcc; six `BENCH OK`; `make lint OK`; `test-negative`'s `0 passed, 1 failed` is the expected harness self-check
- [x] Line budget respected: Ist 112 / Soll ~150 (excluding HTML comments)
- [x] Status set to `passed`
