# Review Report: game-loop

| | |
|---|---|
| **Phase** | Review |
| **Owner** | Reviewer (`/peer-review`) |
| **Input** | The uncommitted working-tree diff of `/increment`, `.spark/game-loop/plan.md` |
| **Status** | `passed` |
| **Round** | 2 |
| **Date** | 2026-09-02 |

**Handoff**
- **Status:** mirrors the header table above (authoritative for `Status`).
- **Verdict:** All nine Round-1 findings confirmed fixed against the reviewer's own repros; the determinism proof's negative control is now armed for both input fields. Three new low-severity findings stay open; none blocks the gate.
- **Open:** `0 open` — all 13 findings fixed. F1–F9 `fixed r2` from the Round 2 re-review (each re-derived, not accepted on the developer's claim); F13 fixed by the reviewer during Round 2. F10–F12 fixed in `/increment` fix-mode after Round 2: F10's doc now states precisely which signatures compile but must not be written (by-value/`const&` `render`), F11's token list extended to catch `<time.h>`/`<sys/time.h>`/`clock_gettime`/`steady_clock`/`system_clock`/`high_resolution_clock` (reviewer's exact repro re-run and now caught), F12's `docs/host-tests.md` wording no longer applies the 5 ms budget to the non-gated comparison cost. `make clean && make test-all` green after: 91 C++ tests × 4 configurations, 3 benches `BENCH OK`, 15 Python + 2 round-trip, sips OK, lint OK.
- **Binding ruling:** §6 Verdict and the gate checklist below — the only binding location.
- **On conflict:** the numbered body below wins for everything except `Status`; log the mismatch as a finding at the next `/peer-review` and proceed — don't stop on it.

## 1. Scope

Round 2 re-review of the full working-tree diff on top of Round 1's baseline: `firmware/steamcore/include/steamcore/game_loop.h`; `firmware/steamcore/test/{game_loop_test.cpp, game_loop_determinism_test.cpp, fb_compare.h, fb_compare_test.cpp, replay_fixture.h, bench_game_loop.cpp}`; the diffs to `Makefile`, `tools/check_constraints.sh`, `docs/host-tests.md`; and this round's artifact changes to `.spark/game-loop/plan.md` and `.spark/constitution.md` (§2 + `## Amendments`). Read in context: the whole lint script, `Makefile`, README's Phase-2 checklist, the `library` lens.

Not reviewed, per the task scoping: `README.md` (pre-existing unrelated change), `assets/**` (untracked, unrelated provenance). No tool file was passed, so scoping was done by hand from `git status --short` — no blast-radius query was run and none is cited.

Verified by running, not by reading. `make clean && make test-all` green from scratch, twice: 91 C++ tests × 4 configurations, 3 benches `BENCH OK`, 15 Python + 2 round-trip, `sips` OK, `make lint` OK. Every Round-1 repro was re-executed against the current tree, not accepted from the developer's summary: the three F1 fixture mutations plus a fourth of my own (drop the `fire` branch); eleven F2 compile probes; an independent `-Wpedantic -pedantic-errors` build of the game-loop file set and of the header's usage example verbatim; both F6 missing-file repros (`game_loop.h` and, separately, `bench_game_loop.cpp`, to reach the allocation loop alone); a new `<time.h>`/`clock_gettime` injection into `replay_fixture.h` (F11). `make test-asan FILTER=game_loop` re-run for AC-1.4/AC-4.3 (Must-AC verification — re-derivation condition (b)), 14 passed, 0 findings. All mutations reverted; game-loop file shasums confirmed back to their pre-review values apart from my own F13 fix, after which the full gate was re-run green.

## 2. Plan Conformance

| Task | Implemented as planned? | Note |
|---|---|---|
| T1 | ✅ r2 | The `is_invocable`/`decltype(&Game::update)` approach was replaced by the standard C++17 `void_t` detection idiom (`game_loop.h:54-66`). Re-derived against eleven probes: missing, overloaded-with-no-match, private and wrong-parameter-type `update` each now give only the clean named `static_assert` at `:94`; templated `update`, a genuinely valid overloaded `update` and `render(Framebuffer&, int = 0)` are correctly accepted. The DoD's "a wrong signature" generalisation now holds for every case that can be detected — the residual over-promise is F10. |
| T2 | ✅ | N = 0/1/100, counts plus a strictly-alternating event log. `checkStrictInterleaving` is the real assertion; M1 confirms it fails on a swapped order. |
| T3 | ✅ | Distinct-value-per-index and four-combination tests; `sizeof` `static_assert` confirmed to break on an added third field. |
| T4 | ✅ | Address identity over 100 ticks, two-pixel no-clear, update-before-render visibility — all three fail under their matching mutation. |
| T5 | ✅ | `framebuffersEqual` scans all 38,400 positions through public `pixel()`; proven failing at (0,0), last pixel and interior. Re-exercised live this round: it is the oracle that reported every F1 divergence. |
| T6 | ✅ r2 | The sensitivity half now meets its own DoD wording. One test per field via a shared helper, plus a third test asserting `replayInputAt` really yields all four combinations. Each of my four fixture mutations fails a **new** test, never merely "no test left to fail": dropping `if (input.start)` → `..._sensitive_to_start_changes`; dropping `if (input.fire)` → `..._sensitive_to_fire_changes`; collapsing `replayInputAt` to one constant → `..._covers_all_four_combinations`; both together → two failures. |
| T7 | ✅ r2 | Missing-file handling is now `report`-and-fail on **both** loops. Re-derived: moving `game_loop.h` aside fails `make lint` with both rules naming the exact path; moving `bench_game_loop.cpp` aside (which only the allocation loop names) fails it too — the loop I recorded in Round 1 as having no guard at all. Header comment updated to say the script now reaches `test/`. Residual coverage gap in the rule's token list is F11 (implementation matches T7's DoD list exactly; the list itself is short). |
| T8 | ✅ r2 | Bench label now reads "comparison cost for one framebuffersEqual call", which is exactly what is timed. `docs/host-tests.md:47-56` records the value, date, host and scope; measured 0.0008–0.0017 ms (ticks) and 0.1725–0.4027 ms (one comparison) over five runs on this host, so the recorded `~0.0009`/`~0.25` are honest representatives. One phrase in that entry is F12. |
| T9 | ✅ r2 | All six NFR-7 doc points present; usage example still compiles verbatim, now also under `-Wpedantic -pedantic-errors`. The NFR-6 audit in T9's own DoD names `detail::kGameHasUpdate`/`kGameHasRender` explicitly instead of folding them into "everything else", and plan §1 Decision 2 now states the real difference from `clip.h` — same convention, not the same reachability — rather than reasserting equivalence in softer words. Judged honest. |

Both approved deviations (`render(Framebuffer&)`; stepper rather than batch `run()`) are implemented exactly as approved. The constitution drift they left behind (F9) is closed: `.spark/constitution.md` §2 now states the contract as `update(const GameInput&)`, `render(Framebuffer&)` with the AC-2.2 reasoning inline and `onCollision(Entity&, Entity&)` marked not yet implemented, logged in `## Amendments` dated 2026-09-02. Every factual claim in that amendment was re-derived, not cited: no `Entity` symbol exists anywhere under `firmware/` or `games/`; README:617 still has "Collision System" unchecked; `game_loop.h:105-108` really does call `update(const GameInput&)` then `render(Framebuffer&)`. The `update(GameInput)` → `update(const GameInput&)` change goes one step beyond what F9 asked for and is correct — it describes the shipped call, and the task records the user approving that extension.

## 3. Findings

| # | Severity | Location | Finding | Status |
|---|---|---|---|---|
| F1 | Major | `firmware/steamcore/test/game_loop_determinism_test.cpp:76` | The sensitivity control diverges only `fire`, so the fixture's `start` half and its input coverage are unguarded. Three mutations leave the entire suite green (12/12): (a) `replay_fixture.h:49` deleting the `if (input.start)` line; (b) `replay_fixture.h:24` making `replayInputAt` return one constant combination, silently voiding AC-4.1's "mixing all four"; (c) both together. **Why it matters:** this is the project's own top-listed risk (plan §5 R1) and T6's DoD asserts "a consumer that ignored its input could not pass" — false for half the input. A future edit dropping `start` handling ships with `make test` green and constitution §4's determinism guarantee certified by a test that cannot see it. **Fix:** diverge `start` at one tick *and* `fire` at another (verified: flipping `inputB.start` at tick 26 makes mutation (a) fail as `game_loop_replay_fixture_is_sensitive_to_input_changes`), plus an assertion that `replayInputAt` actually yields all four combinations across `kReplayTicks`. | fixed r2 |
| F2 | Minor | `firmware/steamcore/include/steamcore/game_loop.h:45-51`, doc claim at `:72-74` | `decltype(&Game::update)` is itself ill-formed whenever the member is absent, overloaded, templated or private, so the named `static_assert` is unreachable in exactly those cases. Verified: all four give `error: no member named 'update' in 'G'` / `reference to overloaded function could not be resolved` at `game_loop.h:47` plus `static_assert expression is not an integral constant expression` — the template-expansion wall the design exists to prevent. It also rejects a legitimate `render(Framebuffer&, int = 0)` and accepts a non-drawing `render(const Framebuffer&)`. **Why it matters:** NFR-7 contract clarity; a missing/misspelled `update` is the commonest consumer mistake and gets the worst diagnostic. **Fix:** detect the *expression*, not the member pointer — `std::void_t<decltype(std::declval<Game&>().update(std::declval<const GameInput&>()))>`. Verified against all eight cases: well-formed everywhere, still rejects missing/private/wrong-type/non-const-ref, and stops rejecting the overloaded, templated and defaulted-arg consumers. Then drop or qualify the doc's blanket "a wrong signature is a compile error" claim. | fixed r2 |
| F3 | Minor | `game_loop.h:21`; `game_loop_test.cpp:185-188,210`; `replay_fixture.h:26-33` | `GameInput{.start = ...}` designated initializers are C++20, used under `-std=c++17`. Ten sites, all new in this increment — the first use anywhere in the repo. Apple clang 14 accepts them silently (even under `-pedantic-errors`), so the gate cannot see it, and `/usr/bin/g++` is clang, so `make test-gcc` cannot either. **Why it matters:** constitution §3 makes C++17 one dialect for host *and* device precisely so "code that compiles for the test gate compiles for the device"; a real GCC/xtensa build warns under `-Wpedantic`, and the affected line in `game_loop.h` is the usage example future game authors will copy. Nothing in spec or plan records this as an accepted extension. **Fix:** either use `GameInput in; in.start = true;` at those sites, or record the extension as accepted in the plan and add `-Wpedantic` to the game-loop compile so a real GCC's diagnostic actually surfaces. | fixed r2 |
| F4 | Minor | `firmware/steamcore/test/bench_game_loop.cpp:61,73` | The `comparison cost (not part of the budget): %.4f ms` line times **one** `framebuffersEqual` call but is printed directly under `for 53 ticks x 2 runs`, inviting a reading 53× too low. Measured on this host at `-O2`: the actual AC-4.1 run (53 lockstep ticks + 53 full comparisons) is **16.68 ms**, against the 0.1724 ms the bench prints. **Why it matters:** plan §5 R5's whole mitigation is "the reviewer sees both numbers"; QA transcribing this into `qa.md` records a flattering figure (constitution §4 honest-status bar). The *scoping* — gating `tick` only — is approved and fine. **Fix:** label it "per comparison" (plan T8's own wording was "per-tick comparison cost"), or time the full `kReplayTicks` loop and print that total. | fixed r2 |
| F5 | Minor | `docs/host-tests.md:33-45` | Plan §4 requires NFR-1's "measured value and host are recorded in `docs/host-tests.md`". The `Benchmark result (NFR-1)` section still records only rendering-core's dirty-scan number; game-loop's 0.0008 ms is absent (as is text-rendering's, pre-existing). **Why it matters:** the doc is where QA and the next reviewer look for the NFR-1 evidence; without it the only record is transient bench stdout. **Fix:** add a game-loop line in the existing entry's shape — value, date, host, `-O2`, and what the budget covers. | fixed r2 |
| F6 | Minor | `tools/check_constraints.sh:137,155` | `[ -f "$f" ] || continue` silently skips a named-but-missing file; the `game_loop_determinism_scanned` guard only fires when *all six* are gone, and the allocation loop has no guard at all. Verified: moving `game_loop.h` aside leaves `make lint OK` with the mechanism unscanned. **Why it matters:** this is the exact false-OK class the script's own header (`:10-15`, review F15) was written to prevent, and a rename during a later refactor is the realistic trigger. **Fix:** in an explicitly-named file set, a missing entry is a script bug — `report` and fail, the same posture as the directory guard at `:22-27`. Also update the file header comment (`:2-5`, "the delivered engine tree") now that the script reaches `test/`. | fixed r2 |
| F7 | Minor | `.spark/game-loop/plan.md` §1 Decision 2 / §2; `game_loop.h:39-53` | The plan justifies `steamcore::detail` in a public header as "not public surface ... matching `clip.h`". Not equivalent: `clip.h` lives in `src/`, unreachable from the single `-I include` path, whereas `detail::kGameHasUpdate`/`kGameHasRender` sit in `include/steamcore/game_loop.h` and *are* reachable by any consumer. **Why it matters:** NFR-6 says "any other new public symbol is a review finding", and the audit records "nothing else" — the honest count is two conventionally-private but reachable variable templates. The convention itself is standard and unavoidable for a header-only template; only the stated precedent and the audit wording are wrong. **Fix:** correct the precedent claim and record the two `detail` symbols explicitly in the NFR-6 audit. | fixed r2 |
| F8 | Nit | `game_loop.h:3` | `#include <cstdint>` is unused — no fixed-width type appears anywhere in the header. Left open rather than fixed by me because plan §2 names `<cstdint>` as a dependency; removing it silently would create a new plan/code mismatch. **Fix:** drop the include and the plan §2 mention together. | fixed r2 |
| F9 | Minor | `.spark/constitution.md:36-38` | Constitution §2 states the engine↔game contract as `update(GameInput)`, `render()`, `onCollision(Entity&, Entity&)`. The shipped contract is `render(Framebuffer&)` with no `onCollision`. **The code is right** — plan §1 D4 recorded the deviation and the user approved it. The constitution is now stale on the project's own "central contract". **Why it matters:** silent drift here is how the next feature plans against a document that no longer describes the code. **Fix:** `/charter` amendment recording `render(Framebuffer&)` and that collision remains unbuilt. Not a code change. **Fixed:** `.spark/constitution.md` §2 amended — `render()` → `render(Framebuffer&)` with the AC-2.2 reasoning inline, `onCollision(Entity&, Entity&)` marked not yet implemented, and (caught while amending, same drift class) `update(GameInput)` → `update(const GameInput&)` matching the actual `tick()` call. Logged in the constitution's `## Amendments` table, dated. | fixed r2 |

| F10 | Minor | `firmware/steamcore/include/steamcore/game_loop.h:87-89` | The doc still claims flatly "A wrong `update`/`render` signature on `Game` is a compile error". It is not: the detection idiom checks whether the *call expression* is well-formed, so any parameter type the argument converts to passes. Verified by compiling under the project's exact flags — `void render(Framebuffer)` (by value) and `void render(const Framebuffer&)` both build clean with `-Wall -Wextra -Werror`. The by-value case is the dangerous one: the game draws into a 38,400-byte stack copy, silently defeating plan §1 D4's "same instance every tick is structural, not caller discipline", on a target with no dynamic allocation and a small stack. **Why it matters:** NFR-7 is contract clarity, and this is the one sentence a game author will trust instead of testing; it is also the half of F2's own fix instruction ("then drop or qualify the doc's blanket claim") that was not carried out — the detection half was, and is confirmed fixed. **Fix:** qualify the sentence — a `Game` whose `update`/`render` *cannot be called* as `update(const GameInput&)`/`render(Framebuffer&)` is a named compile error; a `render` taking the framebuffer by value or by `const&` still compiles and must not be written. Left open rather than reworded by me: whether to also tighten the check (which would re-reject the templated/defaulted-argument consumers F2 asked to accept) is a design call, not a typo. | fixed |
| F11 | Minor | `tools/check_constraints.sh:149` | The determinism rule's token list matches T7's DoD exactly, and that list is bypassable. `\bclock[[:space:]]*\(` does not match `clock_gettime(`, `\btime[[:space:]]*\(` does not match it either (no word boundary after `get`), and `<time.h>`/`<sys/time.h>` are not in the alternation at all. Verified: injecting `#include <time.h>` plus a `clock_gettime(CLOCK_MONOTONIC, &ts)` helper into `replay_fixture.h` leaves `make lint OK`. **Why it matters:** this rule *is* AC-1.3/AC-4.4's automation, written precisely so the determinism guarantee does not depend on reviewer diligence; the delivered code is clean (re-derived by reading, not by the gate), so nothing is broken today — the gate is just weaker than plan §5 R6 claims. **Fix:** add `<time\.h>|<sys/time\.h>|clock_gettime|steady_clock|system_clock|high_resolution_clock` to the alternation (all safe — `bench_game_loop.cpp` is outside this rule), and update T7's DoD token list to match so plan and script stay in step. | fixed |
| F12 | Nit | `docs/host-tests.md:53-54` | "both stay far inside the 5 ms budget" applies NFR-1's budget to the comparison cost, which the same sentence has just called non-gating and the budget does not cover. Scaled by the 53 comparisons AC-4.1 actually performs, that cost is ~9–21 ms on this host — outside 5 ms, legitimately so. **Why it matters:** this entry is exactly where QA transcribes NFR-1 into `qa.md` (constitution §4 honest status); the preceding clauses are precise, so only the closing flourish invites the F4 misreading at one remove. **Fix:** drop "both" — say the `tick` measurement is inside the budget and the comparison cost is reported for context only, optionally naming the full AC-4.1 cost including its 53 comparisons. Left open rather than fixed by me because the honest fix introduces a measured number, which belongs to whoever runs the bench. | fixed |
| F13 | Nit | `firmware/steamcore/test/game_loop_determinism_test.cpp:60` | `replaySequenceDetectsDivergenceIn` was a namespace-scope helper with external linkage, while every sibling test helper (`game_loop_test.cpp:11-118`, `replay_fixture.h`) is in an anonymous namespace or `steamcore::test`. All test translation units link into one binary, so this is one name collision away from an ODR violation a linker need not diagnose. **Fix:** wrap in an anonymous namespace. **Fixed by the reviewer** — one-line change, no behaviour change; `-Wpedantic` clean, `make clean && make test-all` re-run green afterwards, and the F1 start-branch mutation re-confirmed still failing against the edited file. | fixed r2 |

Verified sound, no finding: `grep -nH` in both new rules is correct and necessary — with plain `grep -n` on a single file the output is `line:content`, which the shared `grep -vE ':[0-9]+:[[:space:]]*//'` exclusion cannot match, so `//`-comment lines would stop being excluded. A real `rand()` in `replay_fixture.h:51` was reported as `firmware/steamcore/test/replay_fixture.h:51:...` and failed the gate; a `std::chrono` mention on a comment line was correctly ignored.

## 4. Requirements Traceability

| Spec ID | Implemented at | Verdict |
|---|---|---|
| AC-1.1 | `game_loop.h:105-108`; `game_loop_test.cpp:53,146,158` | ✅ met — counts + strict interleaving; M1 fails it |
| AC-1.2 | `game_loop_test.cpp:135` | ✅ met |
| AC-1.3 | `game_loop.h` (no clock — re-derived by reading the whole header, not via the gate); `check_constraints.sh:131-154` | ✅ met r2 — the mechanism reads no clock; the gate's token list is narrower than it looks (F11), which weakens future enforcement, not today's compliance |
| AC-1.4 | `make test-asan FILTER=game_loop` → 14 passed, 0 findings | ✅ met (re-derived: Must-AC verification) |
| AC-2.1 | `game_loop_test.cpp:183` | ✅ met — M2 fails it |
| AC-2.2 | `game_loop.h:102,112`; `game_loop_test.cpp:219` | ✅ met — address identity, structural |
| AC-2.3 | `game_loop_test.cpp:236` | ✅ met — M3 fails it |
| AC-2.4 | `game_loop_test.cpp:250` | ✅ met |
| AC-3.1 | `game_loop.h:36`; `game_loop_test.cpp:172` | ✅ met — a third field breaks the build (verified) |
| AC-3.2 | `game_loop_test.cpp:205` | ✅ met |
| AC-3.3 | `game_loop_test.cpp:183`; `replay_fixture.h:24` | ✅ met |
| AC-4.1 | `game_loop_determinism_test.cpp:21,109,114,125`; `fb_compare.h:18`; `replay_fixture.h:20` (53 ≥ 50) | ✅ met r2 — positive proof sound; the negative control is now armed for `start`, for `fire` and for the input sequence's own coverage, each proven by a mutation failing a distinct new test |
| AC-4.2 | `replay_fixture.h` — no randomness at all; `check_constraints.sh:139` | ✅ met |
| AC-4.3 | `make test-asan FILTER=game_loop` → 0 findings | ✅ met (re-derived: Must-AC verification) |
| AC-4.4 | `check_constraints.sh:131-154` | ✅ met — bench exclusion is plan-recorded and consistent with NFR-5 ("no engine or fixture code") |
| NFR-1 | `bench_game_loop.cpp`; 0.0008–0.0017 ms vs 5 ms over five runs; `docs/host-tests.md:47-55` | ✅ met as scoped (tick calls only, approved) — value and host now recorded; one phrase in that record is F12 |
| NFR-2 | `game_loop.h:111-112` (two references); `check_constraints.sh:155-171` | ✅ met — gate re-observed failing on injected `make_unique`; missing-file guard now covers this loop too |
| NFR-3 | ASan/UBSan across all 91 tests | ✅ met |
| NFR-4 | `make test` + `make test-gcc` clean, `-Wall -Wextra -Werror`; additionally verified clean under `-Wpedantic -pedantic-errors` | ✅ met r2 — zero designated initializers remain anywhere in `firmware/` (only a comment naming the construct); C++17 dialect honestly one dialect again |
| NFR-5 | `game_loop_determinism_test.cpp:21`; `Framebuffer::Framebuffer()` = `clear(BLACK)`, so a fresh buffer is deterministic | ✅ met r2 — negative control closed with AC-4.1; two-compiler claim still honestly reported as nominal (`/usr/bin/g++` is clang on this host) |
| NFR-6 | `game_loop.h`: public = `GameInput{start,fire}`, `GameLoop<Game>`, `tick`; `detail::kGameHasUpdate`/`kGameHasRender` reachable but conventionally private | ✅ met r2 — re-audited symbol by symbol after F7; plan §1 D2 and T9 now both name the two `detail` symbols and state the `clip.h` difference truthfully. `test/` headers stay unreachable (single `-I include`, confirmed on the live compile line) |
| NFR-7 | `game_loop.h:8-22` (example), `:26-30`, `:70-91` | ⚠️ partial r2 — all six required statements present and the example compiles verbatim under `-Wpedantic`; the compile-error sentence still over-promises (F10, superseding F2's doc half) |

## 5. What Was Checked

- [x] Correctness: logic does what the acceptance criteria demand
- [x] Non-functional: applicable NFRs and constitution quality bars hold
- [x] Error handling: no failure path exists (no throw, no error code, no I/O) — inherited contract, documented and honoured
- [x] Security: N/A per NFR-9 — no input persisted, no external surface, no secret; nothing in the diff changes that
- [x] Tests: exist, pass, and were proven to fail under 17 separate injected defects across both rounds
- [x] Readability: the next developer will understand this
- [x] Library lens (constitution §2, scoped): public surface minimal and intentional (three symbols, one entry point); no removal or signature change to any existing export, so the semver/packaging checks stay the no-ops the constitution declares; the one new dependency line is two stdlib headers, both used; contract docs carry a compilable example — the one lens gap is F10, tied to NFR-7

## 6. Verdict

Passed. Every one of the nine Round-1 findings is genuinely fixed, and I confirmed each against my own repro rather than the developer's summary — the F1 fix is the one that mattered and it is the real thing, not a test deleted and replaced with a friendlier one: the three mutations that used to leave the suite green (drop `ReplayGame`'s `start` branch, collapse `replayInputAt` to one constant, both) now each fail a *new, named* test, a fourth mutation of my own (drop the `fire` branch) fails the matching one, and the four-combination assertion re-derives the input coverage from the fixture instead of trusting its switch statement. The `void_t` rewrite closes all four blind spots I found in the signature check while correctly accepting the templated, overloaded and defaulted-argument consumers I said it was wrongly rejecting; the designated initializers are gone and I verified `-Wpedantic -pedantic-errors` cleanliness myself instead of taking the claim; both lint loops now fail loudly on a missing named file, including the allocation loop that had no guard at all; and the constitution amendment describes what actually shipped, including the `update(const GameInput&)` correction that went one honest step beyond what I asked for. What stays open is small and none of it blocks: the header still tells a game author flatly that "a wrong signature is a compile error" when `render(Framebuffer)` by value compiles clean and silently draws into a 38 KB stack copy (F10 — the doc half of F2's own fix, not carried out); the determinism lint rule's token list lets `#include <time.h>` plus `clock_gettime` straight through, which I demonstrated (F11 — the shipped code is clean, the future gate is weaker than the plan claims); and one clause in `docs/host-tests.md` applies the 5 ms budget to a number that budget does not cover (F12). I fixed one Nit of my own finding (F13, a test helper with external linkage in a single-binary suite) and re-ran the full gate green afterwards. Two Minors and a Nit are a fair handoff to QA and the next increment; the mechanism, its determinism proof and its negative controls are sound.

---

## ✅ REVIEW GATE

*All boxes checked → `/demo-day` may start. Any box open → back to `/increment`. On
re-review, edit this same checklist in place — never duplicate it as a second gate.*

- [x] No open Blocker findings
- [x] No open Major findings — F1 (the only Major) confirmed fixed r2; nothing waived, nothing downgraded
- [x] Every Must AC traces to implementing code; no constitution non-negotiable violated (no dynamic allocation, no GPIO literal, no full-frame push, no hardware-verification claim)
- [x] All plan deviations documented and accepted (both approved deviations implemented exactly as approved; every task now conforms to its DoD — the three open findings are F10–F12, of which only F11 is a plan-side gap, in T7's token list)
- [x] Test suite runs green — `make clean && make test-all`, 91 tests × 4 configs, 3 benches `BENCH OK`, 15 Python + 2 round-trip, `sips`, lint, all green, re-run twice by the reviewer and once more after the F13 fix
- [x] Line budget respected: Ist 118 / Soll ~150 (excluding HTML comments)
- [x] Status set to `passed`
